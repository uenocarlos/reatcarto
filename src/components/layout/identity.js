/** Classes compartilhadas da identidade visual (padrão Meus Mapas). */
export const IDENTITY_CARD_CLASS = 'border-l-4 border-l-primary';

/** Barra laranja das telas de app: mesma largura e altura em todas as rotas. */
export const APP_HEADER_INNER_CLASS =
  'mx-auto flex w-full max-w-6xl min-h-[7.5rem] items-center px-4 py-6';

/** Barra laranja de workspace (editor / mapa público). */
export const WORKSPACE_HEADER_CLASS =
  'min-h-[3.75rem] shrink-0 bg-primary px-4 py-3';
